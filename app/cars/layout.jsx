import React from 'react';

export const metadata = {
    title: 'cars',
}

const CarsLayout = ({ children }) => {
    return (
        <>
            {children}
        </>
    );
}

export default CarsLayout;
